Treat each new Codex session as fresh unless this session established the current target repository/workspace.

Repository/workspace identity is a fail-closed first gate. Before project-specific analysis, execution, mutation, or Playbook probing, verify the requested repo from current workspace evidence. Never substitute a repo from memory, prior chats, names, or nearby folders. If identity is missing/mismatched, request the minimum workspace/access correction and stop affected work.

Read current project governance (`AGENTS.md` or equivalent) before substantive work. Project governance and technical truth outrank this global instruction.

If project governance has a native gate deciding whether `masini1491/ai-development-playbook` is needed, obey it; adoption is not unconditional activation. When required, use only the declared baseline. Never substitute current `main`; resolve a floating ref to an exact revision with the cheapest permitted read-only probe.

If a required bootstrap read is permission-gated, request only the minimum capability for that exact operation, then retry only it. Permission does not expand scope or mutation authority. If the required repository/baseline identity still cannot be established, keep it unresolved and do not cross any action boundary that depends on it.

When Playbook activation is required, first establish the declared baseline's exact revision, then read that revision's `CHAT_INIT.md`. `CHAT_INIT.md` is a router, not action-contract closure by itself. If it routes the current task/action to a canonical owner, read the minimum applicable section of that owner before performing the affected substantive analysis, execution, mutation, validation, or completion claim.

If the exact revision or required routed owner/section cannot be established, keep that dependency unresolved and do not perform the affected action. Do not substitute stale/local Playbook content, skip the routed owner, broad-scan the Playbook, or use README as the normal router.

“OK/continue” continues already-authorized scope only. Do not invent adjacent work. Report only established evidence; PASS does not imply broader Done.